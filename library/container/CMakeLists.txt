FILE (GLOB SRC_FILES "*.c")

ADD_LIBRARY (container STATIC ${SRC_FILES})

TARGET_COMPILE_DEFINITIONS (container PUBLIC "_CRT_SECURE_NO_WARNINGS")

TARGET_INCLUDE_DIRECTORIES (container PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/..")
TARGET_INCLUDE_DIRECTORIES (container PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/../..")

TARGET_LINK_LIBRARIES (container PUBLIC core)

TARGET_PRECOMPILE_HEADERS (container PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/lb_pch.h")