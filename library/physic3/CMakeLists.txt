FILE (GLOB SRC_FILES "${CMAKE_CURRENT_SOURCE_DIR}/src/*.c")

ADD_LIBRARY (physic3 STATIC ${SRC_FILES})

TARGET_COMPILE_DEFINITIONS (physic3 PUBLIC "_CRT_SECURE_NO_WARNINGS")

TARGET_INCLUDE_DIRECTORIES (physic3 PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")

TARGET_LINK_LIBRARIES (physic3 PUBLIC math)

TARGET_PRECOMPILE_HEADERS (physic3 PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include/physic3/lb_pch.h")