ADD_LIBRARY (math INTERFACE)

TARGET_COMPILE_DEFINITIONS (math INTERFACE "_CRT_SECURE_NO_WARNINGS")

TARGET_INCLUDE_DIRECTORIES (math INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/include")

TARGET_PRECOMPILE_HEADERS (math INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/include/math/lb_pch.h")