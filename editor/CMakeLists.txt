FILE (GLOB C_SRC_FILES "*.c")
FILE (GLOB CXX_SRC_FILES "*.cpp")

ADD_EXECUTABLE (editor ${C_SRC_FILES} ${CXX_SRC_FILES})

TARGET_LINK_LIBRARIES (editor PUBLIC engine)

TARGET_PRECOMPILE_HEADERS (editor PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/ed_pch.h")